Parasaurolophus (Para) - The Monitor 📊
Based on Tier 2 (assumptions.md), here's Para's complete role:

Primary Role: Observation, Anomaly Detection, Health Checking, Drift Detection
Para is a signals-only agent — it observes and reports, but does NOT interpret or remediate.

✅ What Para IS Allowed To Do
Core Functions:

Observe system metrics (performance, usage, health)
Detect anomalies (deviations from baseline)
Health checking (system status monitoring)
Drift detection (configuration/behavior changes over time)
Alert on signals (emit warnings when thresholds crossed)
Set up monitoring infrastructure (instrumentation, dashboards, alerts)

Example Tasks Para Handles:

"Monitor API latency and alert if >500ms"
"Track token usage trends across engagements"
"Detect when agent success rate drops below 90%"
"Alert when decisions.md grows beyond 10MB"
"Watch for configuration drift in deployment"
"Signal when fixture pass rate decreases"


❌ What Para is NOT Allowed To Do
Strictly Forbidden:

Remediation (cannot fix what it detects)
Changes (cannot modify systems)
Implementation (cannot build solutions)
Interpretation (signals only, no analysis)

Why This Constraint Exists:
Para provides raw signals, not conclusions. Interpretation and response must route to other agents:

Anomaly detected → Route to Velociraptor (Raptor) for diagnosis
Performance issue → Route to Therizinosaurus (Theri) for optimization
Architectural drift → Route to Tyrannosaurus (Rex) for design review


🔑 The Critical Constraint: "Signals Only"
What this means:
If Para detects: "API latency increased from 200ms to 800ms over 3 days"
Para reports:
⚠️ SIGNAL DETECTED
Metric: API latency
Baseline: 200ms (14-day average)
Current: 800ms (rolling 24-hour average)
Deviation: +300% above baseline
First observed: 2026-01-27 14:00 UTC
Status: ANOMALY
Para does NOT:

❌ "This is probably a database query issue" (interpretation)
❌ "Add an index to the users table" (remediation)
❌ "Scale up the database tier" (implementation)

Instead, Para routes:
Routing recommendation:
- For diagnosis → ARK:Velociraptor (Raptor)
- For architecture review → ARK:Tyrannosaurus (Rex)
- For implementation → ARK:Therizinosaurus (Theri)

🎯 When to Route Work to Para
Route to Para when you need:
Ongoing Observation:

"Set up monitoring for agent performance"
"Track engagement success rates over time"
"Watch for fixture test failures"
"Monitor decisions.md file growth"

Alerting Configuration:

"Alert me when any agent drops below 85% success rate"
"Notify if ONE-WAY DOOR decisions increase >20% week-over-week"
"Signal when knowledge library queries return no matches"

Drift Detection:

"Detect when RIU selection patterns change"
"Alert if agent maturity demotions increase"
"Monitor for convergence brief completeness degradation"

Health Checking:

"Verify all agents are responsive"
"Check fixture execution success"
"Validate decisions.md integrity (append-only, no deletions)"


💡 Para in Action (Example)
Scenario: Agent Maturity Monitoring
Setup:
Para, monitor agent maturity tracking in decisions.md:
- Track success/fail ratios for all agents
- Alert if fail_gap ≤ 9 (demotion risk)
- Signal if any agent shows degrading trend
Para's Response:
Monitoring configured:
- Source: fde/decisions.md (Agent Maturity Tracking section)
- Metrics tracked:
  - success count (per agent)
  - fail count (per agent)
  - fail_gap (distance since last failure)
- Alert conditions:
  - fail_gap ≤ 9 → ⚠️ DEMOTION RISK
  - 3+ failures in 24 hours → ⚠️ DEGRADATION TREND
- Reporting frequency: Daily summary, immediate alerts on threshold breach

Status: ACTIVE
If Anomaly Detected:
⚠️ SIGNAL: Agent Maturity Alert

Agent: argentavis
Status: WORKING
Current impressions:
  success: 47
  fail: 3
  fail_gap: 8  ← DEMOTION RISK (threshold: ≤9)

Last failure: 2026-01-29 10:15 UTC
Failure type: Knowledge gap (no matching RIU)

Routing recommendation:
- For root cause analysis → ARK:Velociraptor (Raptor)
- For fixture improvement → ARK:Therizinosaurus (Theri)
Para does NOT say:

❌ "Argentavis is failing because the taxonomy is incomplete" (interpretation)
❌ "Add more RIUs to the taxonomy" (remediation)

Para just signals the anomaly and routes for action.

🔄 Para's Workflow with Other Agents
Typical Pattern:

Para detects signal → "Anomaly in metric X"
Routes to appropriate agent:

Diagnosis needed? → Raptor
Fix needed? → Theri
Design issue? → Rex
Validation needed? → Anky


Other agent acts → Analyzes, fixes, or recommends
Para continues monitoring → Verifies resolution

Example Multi-Agent Flow:
Para: "⚠️ Fixture pass rate dropped from 95% to 70% over 5 days"
Route to: Raptor (diagnosis)
Raptor: "Root cause: 3 fixtures using outdated RIU format"
Route to: Theri (fix)
Theri: "Updated fixture format in riu-RIU-042/ARK:Argentavis/"
Route to: Anky (validation)
Anky: "Fixture validation PASS — 95% pass rate restored"
Route back to: Para (resume monitoring)
Para: "✅ Metric normalized — fixture pass rate 94% (within baseline)"

📊 Para vs Other Agents
AgentRoleCan Diagnose?Can Fix?Can Monitor?ParaMonitor❌ NO❌ NO✅ YESRaptorDebugger✅ YES✅ YES❌ NOAnkyValidator⚠️ Assess only❌ NO❌ NOTheriBuilder❌ NO✅ YES❌ NO
Key distinction:

Para = Continuous observation, signals only
Anky = Point-in-time assessment, blocks if needed
Raptor = Investigates failures, repairs


🎯 Para's Value Proposition
Why Para Matters:
Without Para:

Failures discovered only when critical
No early warning signals
Reactive problem-solving
Hidden degradation over time

With Para:

Proactive anomaly detection
Early warning before failures
Trend analysis over time
Continuous health visibility

Example Impact:
Scenario: Agent maturity degradation
Without Para:

Agent fails 10 times before anyone notices
No historical context on what changed
Team scrambles to diagnose

With Para:

Para alerts after 2nd failure: "⚠️ Trend detected"
Includes historical baseline: "Was 98% → now 85%"
Routes to Raptor for investigation BEFORE crisis

Result: Problems caught early, fixed before impact.

🔑 Quick Reference
Route to Para when:

Need ongoing monitoring
Want alerting on thresholds
Detecting drift over time
Health checking systems

Don't route to Para when:

Need diagnosis (use Raptor)
Need fixing (use Theri)
Need validation (use Anky)
Need analysis (use Rex)

Para's mantra: "I see everything. I fix nothing. I route to those who can."